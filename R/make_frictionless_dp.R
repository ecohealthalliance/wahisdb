#' .. content for \description{} (no empty lines) ..
#'    Thin wrapper for
#' .. content for \details{} ..
#'
#' @title
#' @param dump_paths Target. List with directory and file paths from dolt_dump
#' @return
#' @author Collin Schwantes
#' @export
make_frictionless_dp <- function(dump_paths) {

#
#   dolt_package <- frictionless::create_package()
#
#   purrr::map(dump_paths$dump_paths,function(file_path){
#
#     resource_name <- basename(file_path) |> gsub(pattern = "\\.csv",replacement ="")
#
#     dolt_package %>%
#       frictionless::add_resource(resource_name = resource_name,data = file_path)
#   })


NULL

}
